apiVersion: v1
kind: Service
metadata:
  name: langfuse-svc
  namespace: lqoasis-aut0
spec:
  selector:
    app: langfuse-app
  type: NodePort
  ports:
  - name: langfuse-svc
    protocol: TCP
    port: 3003
    targetPort: 3000
